import { useState } from "react";

const Home = () => {
  const [blogs, setBllogsj n]

  return ( 
    <div className="home">
      <h2>Homepage</h2>
      <p>{ name } is {age} years old</p>
      <button onClick={handleClick}>Click Me</button>
    </div>
   );
}
 
export default Home;